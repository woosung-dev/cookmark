# 체크리스트 — #32 (applied-app.jpeg 제안 라벨 배지 재렌더)

브랜치 `design/32-semantic-label-badges` · 베이스 = main · 트랙 = 디자인/실행(wayfinder 맵 #27 밖).

정본 — 디자인 언어 = `DESIGN.md` §2 · 라벨 색 결정 = #29 resolve comment · 화면 구조 = ADR-0001/G1 #8.
대상은 **목업 1장뿐**이다. 코드(`lib/theme/app_colors.dart`·`lib/ui/widgets/suggestion_card.dart`)와
`DESIGN.md` §2는 이미 정본과 일치 — 손대지 않는다.

---

## 0. 조사

- [x] 목업 출처 확인 — 프로토타입은 커밋된 적 없는 스크래치다(773f293은 삭제를 기록만 함). **소스 없는 래스터 1장**이 전부다
- [x] 정본 대조 — `DESIGN.md` §2 토큰 · `app_colors.dart` · `suggestion_card.dart`의 `_LabelBadge`
- [x] 목표 배지 형태 확정 = **tint bg + fg 글자**(테두리 없음). 코드 `_LabelBadge`가 근거
- [x] 대상 배지 인벤토리(4개) — ④에 3개, ⑤에 1개

## 1. 재렌더

- [x] ④ "바로 가능" — 퍼시먼 채움+흰 글자 → go `#E4F1E9` bg / `#1F6B43` fg
- [x] ⑤ "바로 가능" — 동일
- [x] ④ "이것만 사면 가능" — 퍼시먼 아웃라인 → buy `#FBF0DA` bg / `#8A5A12` fg (테두리 제거)
- [x] ④ "부족 · 굴소스" 칩 — 그레이 → danger `#FBE7E2` bg / `#B23A25` fg

## 2. 검증

- [x] 평탄대 스포이드 = 토큰 일치(오차 ≤1/255, JPEG 양자화 바닥)
- [x] 대비 WCAG AA — go 5.57:1 · buy 5.23:1 · danger 5.00:1 (전부 일반 텍스트 AA 통과)
- [x] 변경 범위 — 배지 4개 pill 밖은 재인코딩 노이즈만(채널당 평균 0.25/255, 98%가 ≤2)
- [x] 육안 — ④·⑤ 전체 · 배지 10배 확대 전후 대조

## 3. 문서

- [x] `docs/design/README.md` — "비정본 ⚠️ / 재렌더 follow-up #32" 경고 해소, 잔여 갭(아이콘) 명시
- [x] 컨텍스트 노트 — 방법·실측 좌표·기각안 기록

## 4. 마감

- [x] 커밋
- [ ] `/code-review`
- [ ] 이슈 #32 코멘트 + 닫기

---

## 범위 밖 (의도적)

- **§2·§8 "색+아이콘 이중 신호"의 아이콘** — 목업 배지엔 원래 아이콘이 없다. #32는 **색**만 요구하고,
  #29가 아이콘을 "권장 강화 신호이며 D0 게이트가 아님"으로 이미 규정했다. 아이콘 추가는 pill 폭·텍스트
  재배치를 수반해 글리프 보존 재렌더의 전제를 깨뜨린다. 별도 이슈로 표면화.
- **"애매하지만 가능"(그레이 `#5B5B60`/`#EFEFF2`)** — 목업 6화면에 이 라벨을 쓴 카드가 없다. 재렌더 대상 0건.
- **③ "확인"·"맞아요" 배지, ⑤ "담기" 칩, "이거 했어요"·"다시 제안" 버튼** — 제안 라벨이 아니라
  confidence 컨트롤·액션 어포던스다. 퍼시먼이 정본(§7).
