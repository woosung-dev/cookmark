# apps/api — 계약

이 디렉토리는 비어 있다. 코드가 아니라 좌표다 (ADR-0008).

- **무엇이 들어오는가.** 진짜 백엔드(서버 DB·인증) — 루트 `api/` 프록시의 승계자.
- **어떤 rules가 규율하는가.** `.claude/rules/backend.md` — 내부 레이아웃은 §11을 따르되 `apps/api/src/`로 읽는다.
- **실체화 트리거.** ADR-0005의 "서버 DB·로그인 없음"을 뒤집는 자기 ADR(wayfinder 지도 산출).
