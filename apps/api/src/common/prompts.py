# 프롬프트 정본 — 전부 여기 상수로만 산다. 라우트·서비스 인라인 금지 (backend.md §4)

# 승계: api/extract.mjs promptFor() 문자 그대로. 원본 레시피를 옮기지 않는 것이 저작권 경계다(#13).
# 요리명 미인식 → 빈 배열은 프롬프트가 정의한 정상 출력이다 — 호출부는 []를 실패로 취급하지 말 것.
INGREDIENT_EXTRACTION_PROMPT = """\
"{title}"을(를) 만들 때 보통 들어가는 재료를 한국어로 나열해 주세요.

규칙:
- 재료 이름만 적습니다. 분량·조리법은 적지 마세요.
- 흔한 조리법 기준으로 적습니다. 특정 레시피를 그대로 옮기려 하지 마세요.
- 요리명이 무엇인지 알 수 없으면 ingredients를 빈 배열로 두세요."""
