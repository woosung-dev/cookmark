# contracts — 계약

이 디렉토리는 비어 있다. 코드가 아니라 좌표다 (ADR-0008).

- **무엇이 들어오는가.** API 계약 정본(`openapi.yaml` 등) — 클라이언트 생성물·타입의 상류.
- **어떤 rules가 규율하는가.** 계약 우선 — 스키마 변경이 코드 변경에 선행한다.
- **실체화 트리거.** 프록시 3개(recognize·extract·match)의 암시 계약 문서화는 지금도 가능하고, `apps/api` ADR 시점엔 필수.
