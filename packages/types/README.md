# packages/types — 계약

이 디렉토리는 비어 있다. 코드가 아니라 좌표다 (ADR-0008).

- **무엇이 들어오는가.** 앱 간 공유 도메인 타입(TS).
- **어떤 rules가 규율하는가.** 이름은 `CONTEXT.md` 글로서리, 계약은 `contracts/`가 상류다.
- **실체화 트리거.** 같은 타입을 쓰는 TS 소비자 2개째.
