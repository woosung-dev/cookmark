# packages/api-client-dart — 계약

이 디렉토리는 비어 있다. 코드가 아니라 좌표다 (ADR-0008).

- **무엇이 들어오는가.** 같은 스키마의 Dart 클라이언트.
- **어떤 rules가 규율하는가.** `mobile.md` §8 OpenAPI codegen 행 — Dio 수기 호출과 병존 금지.
- **실체화 트리거.** `contracts/openapi.yaml` 실재 + `apps/mobile` 채택 결정.
