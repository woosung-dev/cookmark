# packages/api-client-ts — 계약

이 디렉토리는 비어 있다. 코드가 아니라 좌표다 (ADR-0008).

- **무엇이 들어오는가.** `contracts/openapi.yaml`에서 생성한 TS 클라이언트.
- **어떤 rules가 규율하는가.** 생성물 규칙 — 수기 수정 금지, 스키마가 상류다.
- **실체화 트리거.** `contracts/openapi.yaml` 실재 + TS 소비자(web/admin) 1개 실체화.
