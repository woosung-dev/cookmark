# packages/design-tokens — 계약

이 디렉토리는 비어 있다. 코드가 아니라 좌표다 (ADR-0008).

- **무엇이 들어오는가.** `DESIGN.md` 토큰의 기계 소비형(JSON 등 파생 생성물).
- **어떤 rules가 규율하는가.** 토큰 정본은 루트 `DESIGN.md`다 — 이 패키지는 파생이지 정본이 아니다.
- **실체화 트리거.** 토큰이 필요한 2번째 UI 소비자 등장 시 `DESIGN.md`에서 생성.
