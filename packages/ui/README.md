# packages/ui — 계약

이 디렉토리는 비어 있다. 코드가 아니라 좌표다 (ADR-0008).

- **무엇이 들어오는가.** 공유 웹 UI 컴포넌트.
- **어떤 rules가 규율하는가.** 루트 `DESIGN.md`(디자인 언어) + `design-tokens` 소비.
- **실체화 트리거.** web·admin 둘 다 실체 + 중복 컴포넌트 실증.
